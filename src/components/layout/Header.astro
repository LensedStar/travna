---
// Header.astro — global site header (TASK-010).
// text logo (brand, --font-heading), primary nav (i18n strings), prominent "Book" CTA -> /rezervacija.
// Reserves slots for the LanguageSwitcher island (TASK-011) and the MobileNav island (TASK-012),
// which mount their React islands here in their own tasks. All visible text comes from the i18n dictionary.
import { useTranslations, defaultLocale } from '../../i18n';
import Button from '../ui/Button.astro';
import LanguageSwitcher from '../../islands/LanguageSwitcher.jsx';
import MobileNav from '../../islands/MobileNav.jsx';

const t = useTranslations(defaultLocale);

const navLinks = [
  { href: '/', label: t('nav.home') },
  { href: '/accommodation', label: t('nav.accommodation') },
  { href: '/activities', label: t('nav.activities') },
  { href: '/menu', label: t('nav.menu') },
  { href: '/contact', label: t('nav.contact') },
];

// i18n stub — non-functional in Phase 1. Options are UI placeholders (selecting does not switch content).
const langOptions = [
  { code: 'si', label: t('lang.si') },
  { code: 'ru', label: t('lang.ru') },
];
---

<header class="site-header">
  <div class="site-header__inner">
    <a class="site-header__brand" href="/">{t('site.name')}</a>

    <nav class="site-header__nav" aria-label={t('a11y.primaryNav')}>
      <ul class="site-header__nav-list">
        {
          navLinks.map((link) => (
            <li class="site-header__nav-item">
              <a class="site-header__nav-link" href={link.href}>
                {link.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>

    <div class="site-header__actions">
      <LanguageSwitcher
        client:idle
        currentLabel={t('lang.current')}
        menuLabel={t('lang.label')}
        options={langOptions}
      />
      <Button href="/rezervacija" class="site-header__book">{t('nav.book')}</Button>
      <MobileNav
        client:idle
        links={navLinks}
        bookHref="/rezervacija"
        bookLabel={t('nav.book')}
        openLabel={t('a11y.openMenu')}
        closeLabel={t('a11y.closeMenu')}
        navLabel={t('a11y.primaryNav')}
      />
    </div>
  </div>
</header>
